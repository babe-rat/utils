import { defineConfig } from 'dumi'

export default defineConfig({
    title: 'utils',
    outputPath: 'dist',
    base: '/utils',
    publicPath: '/utils/',
    favicon: '/utils/logo.png',
    logo: '/utils/logo.png',
    exportStatic: {},
})
